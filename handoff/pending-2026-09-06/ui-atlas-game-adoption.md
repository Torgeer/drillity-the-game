# UI atlas game adoption — active

Private branch codex/ui-atlas-game-adoption, baseline f4e8ad9. Live usage checked37% remaining before first assignment wave. Three agents: consumer/lifecycle (new module + narrow atlas CSS + CPU regression), actual menu integration (menu.js + scoped new CSS), adversarial rendered review (isolated actual-menu harness).

Frozen: blender/ui_atlas.py, public/ui/blender/**, prior atlas source/evidence. No game models copied; no global styles.css/package/build/site/renderer/gameplay/data/shared rig/Claude site edits. No installs/index writes/escalation/push. Scoped patch delivery pending.

Proposed production API mountBlenderAtlas(root,{baseUrl?}) -> {ready,refresh,dispose}; manifest authority reused. Native authored dimensions only; long/dynamic/enlarged text gets measured DOM fallback. Menu/settings disposal and navigation remain production logic.

Actual-game smoke requires root integration checkout/models and GPU lease. Root: please reserve a short actual-menu smoke after patch is reviewed/applied; this private task will separately provide isolated actual menu/components browser evidence. Root owns package/default check/artifact wiring; exact needed script paths will be reported on delivery.

CPU checkpoint: menu.js and scoped menu-atlas.css now implement four navigation faces + four compact quality faces. Play remains its existing full-width56px action with dynamic accessible name synchronized. Menu remount/exit and settings/destroy dispose atlas observers; exit can preserve final decoration. New production consumer exists; async/singleflight/fit tests in progress. Prior frozen atlas gate passed with11corrupt fixtures rejected before scoped CSS additions.

Root-ready actual-game smoke authored at tools/check-ui-atlas-game-smoke.mjs (syntax PASS, not browser-run here). After review/apply, run node tools/check-ui-atlas-game-smoke.mjs --url <served integration base URL> --lease <exact root GPU owner key> --out shots/ui-atlas-game-smoke. Requires actual strict Blender rig source, live context, decoded atlas menu/settings, quality/reduced-motion behavior, navigation/remount and captured response URLs. Full build/artifact check remains root-owned. No package/build config edit needed just to import the consumer: menu.js imports the module and module imports both scoped atlas and existing motion CSS; existing public asset emitter already copies ui/blender/**.

Reviewable CPU checkpoint saved: ui-atlas-game-adoption.patch (13 scoped files; 92,918 bytes) and ui-atlas-game-adoption.sources.json with exact source hashes. `git apply --reverse --check` passes against this private tree. This is explicitly PRE-RENDER, not a final delivery; no commit/index write occurred. Root may inspect the current scoped patch while GPU is occupied. Final browser findings will update these paths before completion. Current request is READY in ui-atlas-game-adoption.gpu-request.md; ownership remains instrument-grade.
