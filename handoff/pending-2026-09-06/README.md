# Preserved candidate work — NOT integrated or accepted

**Later update:** quarry alone has since been approved and integrated after a
texture-readiness capture resolved the gray-rock finding. Read the latest
`research/sites/quarry-live-collar-integration.md` and `handover.md`. This frozen
bundle remains evidence of the earlier delivery/rejection; its patch and hash
files are unchanged. The other three candidates remain unintegrated.

Snapshot prepared 2026-09-06 during Codex usage reserve. These patches are
review inputs, not shipped source. Nothing here is automatically applied by
the build. `snapshot-manifest.json` records exact copied bytes and original
locations. Original worktrees and additional images/generated assets remain
under `C:/Users/henri/Downloads/threads/`; no worktree was deleted.
Local attributes preserve snapshot line endings so recorded hashes survive Git.

Later atlas WIP is preserved separately in `atlas-later-wip/`, with its own
`atlas-later-wip-manifest.json`. Exactly two files differ from the original
13-file inventory: the isolated review checker and its Vite config. All four
production files still match that inventory. These copies are unintegrated
review inputs and have no browser acceptance; their imports/paths refer to
their original worktree layout. The original patch remains unchanged.

| Candidate | Remaining work |
|---|---|
| HUD space | Six-path patch against a3fb994. 862 synthetic-telemetry actual-DOM cases have no reported layout faults, but intended-font access failed and combined current-renderer verification remains due. Preserve newer root site recovery hunks. The reported 48.24–75.32% is DOM scene opportunity, not accepted rendered visibility. |
| Menu atlas adoption | Thirteen-path PRE-RENDER checkpoint against f4e8ad9. App task was waiting on approval. Two private harness files changed after this patch; inspect the original worktree before making a fresh final patch. Read atlas-adoption-root-review.md, resolve its smoke URL concern, and preserve root Continue/aria-label recovery. No rendered acceptance. |
| Quarry collar | Nine-path patch against37f92a4 and matching ignored GLB are CPU reviewed. Root four-case actual-asset capture passed its automated checks, but image inspection found unrelated rocks flat gray in candidate versus textured baseline. Material definitions agree; cause unresolved. DEFER integration pending that investigation. |
| Optimized rig runtime | Five new harness/report paths against763f0a9. Zero of eight runtime cases executed because private preview dependency access failed. Fifteen CPU gate adversaries pass; final identity/device changes still need review. No draw/FPS acceptance. |

Quarry candidate GLB remains at
`C:/Users/henri/Downloads/threads/drillity-quarry-live-collar/public/models/sites/quarry-bench.glb`,
SHA256 `5b53090ed7c92c89be48e4dcf668a7400634aa62eb3d7b89482c4cfc7db78930`.
Author SHA256 is `b0b750e43cd08dc3d0ae76f3322ffdc7848161e90d6b4949e78685c74a1705d4`.
Root preview report at `shots/quarry-collar-root-preview/report.json` lists root
baseline author bytes because the generated candidate was routed read-only;
its `sources` field is not the candidate author manifest. The private manifest
is the author/asset pairing authority. Diagnostic views use a separate camera
and lighting, while `*-phone.png` images are actual gameplay captures.

The rig-runtime files have a `rig-runtime-` prefix in this bundle; their internal
relative links still describe the original private worktree. Other copied
checkpoints also retain historical relative links and status text. This README
and latest `handover.md` clarify current integration status; do not rewrite old
failures as passes. GPU lease is empty after confirmed root browser closure.

Claude's eight site environments remain in its separate active worktree and
are not snapshotted here while Claude owns them. Read `CLAUDE_PARALLEL.md` and
the live `drillity-coordination/claude-sites.md` before integration.
