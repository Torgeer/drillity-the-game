# Quarry live collar — root integration

The quarry's authored shot pattern put capped gravel, stemming, a flag/pin and
one seeded rubble block over the runtime borehole. The source now removes those
five exact objects, leaving the neighboring pattern indices and seeds intact.
No engineered clearance was invented and no shared Claude site source changed.

Author SHA256: `b0b750e43cd08dc3d0ae76f3322ffdc7848161e90d6b4949e78685c74a1705d4`.
Matching ignored GLB SHA256:
`5b53090ed7c92c89be48e4dcf668a7400634aa62eb3d7b89482c4cfc7db78930`.
Root copied the exact verified source/export pair. Git patch application first
changed line endings; normalized content was compared with every private file
before restoring exact delivered bytes. No content mismatch was ignored.

The candidate has955,160 bytes,6 primitives,27,030 vertices and13,840 triangles.
The original65 triangle intersections with the live throat/casing envelope fall
to zero. Root reran the actual exported-geometry gate and18 adversarial cases/
18 permutation assertions. The authoring comparison preserved1,025 complete
survivor signatures and removed exactly5 objects. The package's CPU site checks
now run both quarry gates so later exports must retain the opening.

## Texture readiness finding and actual capture

The original capture `shots/quarry-collar-root-preview` passed automated checks
but showed flat gray candidate rubble. Root and an independent critic rejected
visual acceptance. Its shader warmup plus80 animation frames did not wait for
assets.js's cooperative texture-generation queue. That first evidence remains
unchanged in the pending-work snapshot.

The controlled follow-up changed only the capture's readiness handling, with
the same source and asset bytes. It recorded the earlier checkpoint, then waited
at most60 seconds for a nonempty texture set population, zero pending/failed
tasks and all sets ready. In that run, baseline had17/26 sets ready and9 pending;
candidate had10/26 ready and16 pending. The candidate live blastedRock material
already had map/normal-map bindings, but its matched texture set was not ready.
Both variants reached26/26 ready, zero pending and zero failed.

The four uncased/cased baseline/candidate cases then passed with zero errors,
request failures or lost contexts. Two unpaid-preview warnings are expected
and retained. Session55964 finished exit0 and closed its browser. Root and the
independent critic inspected the candidate uncased diagnostic and cased phone
images: rubble texture is present and the removed center dressing stays absent.
This supports the loading-state explanation; no asset texture repair, runtime
loading fix, FPS improvement or overall art-quality claim is made.

Tracked report: `quarry-live-collar-ready-report.json`; independent review:
`quarry-live-collar-root-review.md`. Images remain local under
`shots/quarry-collar-textures-ready/`. Diagnostic renders use a separate camera
and lighting; `*-phone.png` is actual gameplay. The source map in the preview
report records root's original quarry author because the candidate GLB was
routed read-only before integration. The paired author hash above is separate.

The retained browser tool includes this bounded queue wait and records readiness.
It does not implement every stronger per-frame map-identity suggestion in the
critic's checklist; do not claim it does. To compare with the original again,
pass `--baseline-asset .bak/quarry-original-pre-integration.glb` explicitly,
along with root/candidate/port/lease options; the default identical-file refusal
is intentional after integration. The original private preview denial was not
retried; these captures used root's existing authorized server.

The three other pending candidates remain unintegrated. Historical quarry
DEFER notes in `handoff/pending-2026-09-06/` describe the earlier snapshot;
this report and the new independent review supersede that verdict.

## Final integrated verification

`npm run build`, root session79902, exited0. The expanded CPU suite, headed
reach, Vite and artifact audit all passed. Log: `.bak/integration-build-quarry.log`.
The artifact contains one inline script,19 rigs and56 public assets totaling
53,941,561 external bytes; no teststub. Its quarry GLB hash exactly matches the
reviewed955,160-byte candidate. Index remains about2,933.75kB. The headed browser
closed before packaging; the root build lease was released. Root5198 and the
user-owned5178 servers remain live. No full-quality or fleet FPS certification.
